import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const AddTransactionScreen = ({categories}) => {
  return (
    <View>
      <Text>Add Transactions</Text>
    </View>
  );
};

export default AddTransactionScreen;

const styles = StyleSheet.create({});
